- [Meta OAuth callback URL](meta-oauth-callback.md) — must use Replit dev domain without port; port-suffixed URLs break Meta's redirect_uri validation.
- [Production hardening patterns](production-hardening.md) — null-safe initials, ErrorBoundary, DEFAULT_STATUS_CONFIG fallback, analytics array defaults — all recurring safety patterns in this CRM.
- [API server port conflict](api-server-port.md) — port 8080 zombie processes cause EADDRINUSE on restart; kill PIDs from ps aux before restarting the artifact workflow.
- [Meta Lead Ads sync architecture](meta-lead-sync.md) — pluggable sync service in services/metaLeadSync.ts; scheduled every 15min; dedupes by externalId column; add future platforms alongside runMetaLeadSync().
- [Settings theme override bug](settings-theme.md) — never call setTheme() in a useEffect that watches settings data; next-themes owns the theme via localStorage; only call setTheme() on explicit user clicks.
- [Leads strict isolation](leads-isolation.md) — leads filtered by created_by_id = userId only (no OR IS NULL); analytics raw SQL also strict; all 6 route patterns in leads.ts and 10+ in analytics.ts cleaned up June 2026.
- [Data isolation pattern](data-isolation.md) — leads/deals/analytics filter by userId; use OR col IS NULL for nullable FK columns to handle pre-isolation data; properties uses listed_by_id, deals uses created_by_id.
- [Post-login redirect race condition](post-login-redirect.md) — NEVER call setLocation("/dashboard") after signInWithPassword; let PublicOnlyRoute detect the new session and redirect; manual navigation fires before React re-renders the auth context.
- [Onboarding removed](onboarding-removed.md) — OnboardingGuard deleted; all users marked onboarded=true on first upsert in requireAuth; /onboarding route now redirects to /dashboard; sign-up sends to /sign-in not /onboarding.
- [Google OAuth redirectTo BASE_URL](google-oauth-redirect.md) — redirectTo must include import.meta.env.BASE_URL prefix: `${origin}${BASE_URL.replace(/\/$/,"")}/dashboard`. Without it, Google OAuth lands on root `/` not the app's sub-path.
- [Onboarding save must check response](onboarding-save-check.md) — fetch() does NOT throw on HTTP errors; PUT /api/users/me must check res.ok and throw if failed or onboarded:true is silently skipped causing an infinite redirect loop.
- [i18n system architecture](i18n-architecture.md) — lightweight custom context in src/lib/i18n.tsx; 4 languages EN/UR/PS/SD; RTL via document.dir; localStorage primary; DB via preferred_language in user_settings; LanguageProvider wraps AuthProvider in App.tsx.
- [USD currency format](usd-currency.md) — all currency is USD; formatPKR/formatPKRCompact in currency.ts return $ symbol; compact uses B/M/K; deals.tsx and export-pdf.ts have inline formatCurrency that must also use USD.
- [DB schema must be pushed before API works](db-schema-push.md) — run `cd lib/db && npx drizzle-kit push` on fresh setup; missing tables cause requireAuth catch to return 401 masking the real DB error; requireAuth now returns 500 for DB failures.
- [API route prefix double /api bug](api-route-prefix.md) — app.ts mounts router at /api; route files must NOT include /api prefix (use /org/me not /api/org/me); new route files are easy to write with wrong prefix causing 404.
- [Multi-tenant SaaS schema](multitenant-saas.md) — organizations/plans/payment_requests/audit_logs/organization_addons/ai_usage tables; requireAuth auto-creates org for super admin only; regular users need org assigned; clean up org duplicates with DELETE WHERE id NOT IN (SELECT MIN(id)...).
- [Drizzle sql OR subquery bug](drizzle-or-subquery.md) — complex OR + subquery in drizzle sql template returns 0 rows even when psql returns data; split into two sequential queries (owner lookup then member lookup) to work around it.
